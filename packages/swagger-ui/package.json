{
  "name": "@clubmed/swagger-ui",
  "version": "1.10.1",
  "private": true,
  "homepage": "/dist/latest/swagger-ui",
  "dependencies": {
    "@clubmed/plugins": "1.10.1",
    "@clubmed/webpack": "1.7.1"
  },
  "devDependencies": {
    "serve": "11.3.0"
  },
  "scripts": {
    "start": "cross-env PORT=8081 BROWSER=none cm-scripts start",
    "build": "cm-scripts build",
    "test": "cm-scripts test --coverage",
    "lint:fix": "yarn lint --fix",
    "lint": "eslint --config .eslintrc --cache --quiet \"./src/**/*.{js,jsx}\""
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
