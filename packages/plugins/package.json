{
  "name": "@clubmed/plugins",
  "version": "1.11.2",
  "private": false,
  "main": "src/index.js",
  "dependencies": {
    "@clubmed/components": "1.11.2",
    "@clubmed/webpack": "1.11.2"
  },
  "scripts": {
    "build": "cm-scripts build",
    "start": "cm-scripts start",
    "test": "cm-scripts test --coverage",
    "test:ci": "yarn test",
    "lint:fix": "yarn lint --fix",
    "lint:ci": "yarn lint --format junit --output-file ../../reports/test-results-plugins-lint.xml",
    "lint": "eslint --config .eslintrc --cache --quiet \"./src/**/*.{js,jsx}\""
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "/dist"
}
